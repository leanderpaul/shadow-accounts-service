---
/**
 * Importing npm packages
 */

/**
 * Importing user defined packages
 */
import { Guard } from '@app/lib';

/**
 * Importing components
 */

/**
 * Defining types
 */

/**
 * Declaring the constants
 */
const result = Guard.checkAccess('verified', Astro, '/home');
return result.canAccess ? Astro.redirect('/home') : result.redirect();
---
